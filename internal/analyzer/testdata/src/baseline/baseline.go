// Package baseline provides a minimal fixture for analysistest infrastructure verification.
// This package contains no interfaces or implementations, so no diagnostics are expected.
package baseline

// Exported constant to make this package non-empty.
const Version = "1.0.0"
